import { memo } from "react"
import { useStyles } from "./styles"

export default memo(function BeDelightfulMobileArchived() {
	const { styles } = useStyles()
	return <div>BeDelightfulMobileArchived</div>
})
