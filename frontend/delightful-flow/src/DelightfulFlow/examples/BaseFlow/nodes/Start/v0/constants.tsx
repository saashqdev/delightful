export enum TriggerType {
	// When sending a new message
	Message = 1,
	// When chat window opens
	NewChat = 2,
	// Scheduled trigger
	TimeTrigger = 3,
	// Parameter invocation
	Arguments = 4,
	// Loop start
	LoopStart = 5,
}

