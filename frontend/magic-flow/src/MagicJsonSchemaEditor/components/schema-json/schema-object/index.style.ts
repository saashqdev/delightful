import styled from "styled-components";



export const SchemaObjectWrap = styled.div`
    position: relative;
    .row-line, .col-line {
		position: absolute;
        z-index: 0;
        overflow: visible;
    }
`